class Student:
    def __init__(self, student_id, name, program, completed):
        self.id = str(student_id)
        self.name = name
        self.program = program
        self.completed = completed   
